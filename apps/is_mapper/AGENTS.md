# AGENTS.md

## Makefile-first workflow
- Prefer `make is-mapper-map` over direct npm commands.
- Use `make is-mapper-dev` for mapper development mode.
- Use `npm -w apps/is_mapper run <script>` only when no Make target exists.
